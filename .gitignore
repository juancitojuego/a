# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
.next
build
out

# Turborepo
.turbo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS generated files
.DS_Store
Thumbs.db

# Environment variables
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# IDE directories
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Coverage reports
coverage
.nyc_output

# Optional local config
pnpm-lock.yaml # Often committed, but can be ignored in some strategies if all deps are explicit
# For this subtask, I'll keep pnpm-lock.yaml tracked as it's usually recommended for consistency.
# If the "too many files" error persists for pnpm-lock.yaml, I might need to add it here temporarily.
# However, the error message usually points to node_modules.

# Temporary files
*.tmp
*.temp
temp/
tmp/
